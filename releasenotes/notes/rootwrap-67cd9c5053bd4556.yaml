---
features:
  - |
    The new ``glance_rootwrap_config`` resource has been added. This resource
    can be used to manage contents of ``rootwrap.conf``

  - |
    The new ``glance::config::rootwrap_config`` parameter has been added. This
    parameter accepts arbitrary configuration of ``rootwrap.conf``.
